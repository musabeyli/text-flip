
const flip = require('flip-text')

module.exports = {
  flip: (text) => {
    return flip(text);
  },

  unflip:(text) => {
    //dont have a method for unflipping yet.
    return 'Beep boop, BOP?'
  }

}
